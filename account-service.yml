spring:
  datasource:
    url: jdbc:mysql://localhost:3306/account_service?useSSL=false&serverTimezone=UTC
    username: root
    password: Naveen$1418
    
  jpa:
    hibernate:
      ddl-auto: update   
    show-sql: true
resilience4j:
  circuitbreaker:
    instances:
      customerService:
        slidingWindowSize: 5
        failureRateThreshold: 50
        waitDurationInOpenState: 10s
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
